// Shared utilities across Quick Look extensions.
// This module will contain common code as more extensions are added.

import Foundation

enum SharedConstants {
    static let maxFileSizeBytes = 5 * 1024 * 1024 // 5 MB
}
